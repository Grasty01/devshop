/** 
 * Logique panier (ajout, quantité, total, localStorage) :
 * contient toute la logique métier du panier, séparée de l'affichage. Cela permet 
   de tester la logique (ajouter, retirer, calculer le total) sans dépendre du DOM.
*/
