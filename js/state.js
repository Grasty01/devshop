/**
 * Etat de l'app (produits, panier, filtres actifs) :
 * centralise les données (liste des produits, panier, filtre actif) plutôt que de les
 * éparpiller en variables globales dans plusieurs fichiers — ça évite les bugs de synchronisation
 * entre l'affichage et les données.
 */
